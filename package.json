{
    "name": "tact-workshop",
    "version": "0.0.1",
    "scripts": {
        "start": "blueprint run",
        "build": "blueprint build",
        "test": "npm run build; jest --verbose",
        "test:1": "npm run build; jest --verbose -i HelloWorld.spec.ts",
        "test:2": "npm run build; jest --verbose -i Counter.spec.ts",
        "test:3": "npm run build; jest --verbose -i Calculator.spec.ts"
    },
    "devDependencies": {
        "@ton/blueprint": "^0.14.2",
        "@ton/sandbox": "^0.13.1",
        "@ton/test-utils": "^0.4.2",
        "@types/jest": "^29.5.0",
        "@types/node": "^20.2.5",
        "jest": "^29.5.0",
        "prettier": "^3.1.0",
        "@ton/ton": "^13.9.0",
        "@ton/core": "^0.53.0",
        "@ton/crypto": "^3.2.0",
        "ts-jest": "^29.0.5",
        "ts-node": "^10.9.1",
        "typescript": "^5.3.2"
    }
}
